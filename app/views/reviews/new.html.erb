<!-- Before Ajax %= simple_form_for [@article, @review] do |f| % -->
<%= simple_form_for ([@article, @review], remote: true) do |f| %>
  <%= f.input :content %>
  <%= f.submit "Laisser un commentaire", class: "btn btn-primary" %>
<% end %>
