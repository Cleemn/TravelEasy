<h1>Réserver</h1>

<!-- `form_for([@restaurant, @review])`  POST **`restaurant_reviews_path`** -->
<%= simple_form_for [@article, @booking] do |f| %>
  <%= f.input :start_date %>
  <%= f.input :end_date %>
  <%= f.submit "Réserver" %>
<% end %>
